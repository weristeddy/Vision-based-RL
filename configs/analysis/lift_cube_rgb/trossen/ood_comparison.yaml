# How far the encoder's features move when only the tabletop changes.
# Run default.yaml and ood_wood.yaml first; this reads both feature files.
version: 1
task_id: Mjlab-LiftCube-CollisionCam-DinoV2ViTS14-LocalGrid7-Trossen
checkpoint_file: ckpts/lift_cube/dinov2_vits14_local_grid7_real.pt
output: artifacts/analysis/lift_cube_rgb/trossen
steps:
  - script: comparison
    args:
      left: features.npz
      right: features__wood.npz
      output: comparison/{stage}__{left}__vs__{right}.csv
      plot: plots/comparison__{stage}.png
      stages: [backbone, adapter]
